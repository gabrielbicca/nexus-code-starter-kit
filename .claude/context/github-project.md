# Contexto — GitHub Project

> Gotchas e convenções do GitHub Project (board), Issue Forms, campos e fluxo de cards.
> Carregado **sob demanda** pelo `CLAUDE.md` (import `@`). Remova esta seção se o projeto não usa GitHub Projects.

## Project

| Item | Valor |
|---|---|
| Project | `<#N / URL>` |

## Issue Forms

- `<form>` — campos obrigatórios: `<...>`

## Campos customizados

| Campo | Tipo | Valores |
|---|---|---|
| `<campo>` | `<single-select/text/number>` | `<valores>` |

## Fluxo de cards

- `<coluna A>` → `<coluna B>` : `<quando/quem>`

## Gotchas

- `<armadilha conhecida e como evitar>`
